# here is the module to generate the speech wav, based on the lyrics
# the lyrics are generated using the lyrics module

class TTS:
    def __init__(self, config):
        self.config = config

    def generate(self, lyrics):
        # generate the speech wav file
        pass